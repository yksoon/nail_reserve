import React from "react";

const TermsModal = (props) => {
    const modData = props.modData;
    return (
        <>
            <pre>{modData}</pre>
        </>
    );
};

export default TermsModal;
